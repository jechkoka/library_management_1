<div class="page-header">
  <h1>Modifier un emprunt</h1>
</div>

<div class="form-container">
  <%= render 'form', loan: @loan %>
</div>

<div class="actions">
  <%= link_to 'Voir', @loan, class: 'button show' %>
  <%= link_to 'Retour à la liste', loans_path, class: 'button back' %>
</div>